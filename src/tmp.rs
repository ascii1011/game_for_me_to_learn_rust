
use std::io;


enum CityNames {
    LA,
    SF,
}

/*
impl std::fmt::Display for CityNames {
    fn fmt(&self, f: &mut std::fmt::Formatter) -> std::fmt::Result {
        write!(f, "{:?}[stuff:{}]", self.drugs, self.drugs)
    }
}
*/

fn main() {
    println!(citynames::COUNT);
}